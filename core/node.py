# the core node entrypoint for HyperKube
version_minor = 0
version_major = 0
version_patch = 1

version_string = "0.0.1"
version_status = "alpha"

import os
import sqlite3
from sys import version_info
